package com.sjsu.sister;import android.content.SharedPreferences;import android.os.Bundle;import android.os.Handler;import android.os.Message;import android.widget.Button;import com.sjsu.sister.activity.BaseActivity;import com.sjsu.sister.activity.HomeActivity;import com.sjsu.sister.activity.LoginActivity;public class MainActivity extends BaseActivity {    private Button btnLogin;    private Button btnRegister;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        handler.sendEmptyMessageDelayed(0,1000);//        btnLogin = findViewById(R.id.btn_login);//        btnLogin.setOnClickListener((new View.OnClickListener(){//            @Override//            public void onClick(View v) {//                Intent in = new Intent("com.sjsu.sister.activity.action.LoginActivity" );//                startActivity(in);//            }//        }));////        btnRegister = findViewById(R.id.btn_register);////        btnRegister.setOnClickListener(new View.OnClickListener() {//            @Override//            public void onClick(View v) {//                navigateTo(RegisterActivity.class);//            }//        });    }    private Handler handler = new Handler() {        @Override        public void handleMessage(Message msg) {            getHome();            super.handleMessage(msg);        }    };    public void getHome(){        SharedPreferences sharedPreferences= getSharedPreferences("Sister", MODE_PRIVATE);        Boolean status = sharedPreferences.getBoolean("Status",false);        String userName = sharedPreferences.getString("Name","");        if(status){            navigateTo(HomeActivity.class);        }else{            navigateTo(LoginActivity.class);        }        finish();    }}